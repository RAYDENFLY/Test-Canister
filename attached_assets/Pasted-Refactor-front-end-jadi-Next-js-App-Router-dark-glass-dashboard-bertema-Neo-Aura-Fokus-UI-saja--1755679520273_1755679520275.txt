Refactor front-end jadi Next.js (App Router) dark “glass” dashboard bertema Neo Aura. Fokus UI saja—tanpa backend/dfx. Terapkan hal berikut SANGAT TELITI:

1) Stack & struktur
- Next.js 14 (app dir) + TypeScript + Tailwind.
- Folder: src/app/{page.tsx, jobs/page.tsx, chat/page.tsx, analytics/page.tsx, profile/page.tsx}, src/components/{Sidebar.tsx, Header.tsx, StatCard.tsx, JobCard.tsx, RecentActivity.tsx, MatchingPreview.tsx, ReputationScore.tsx}, src/lib/api_client.ts (mock).
- Providers React Query client di src/components/Providers.tsx.

2) Tema warna (Neo Aura, gunakan tailwind + util class)
- Base bg: #0B1220
- Surface/Glass: backdrop-blur + bg-white/5 + border-white/10 (utility .glass)
- Brand (primary): #179EFF
- Accent success: #10B981
- Accent violet: #8B5CF6
- Warning: #F59E0B
- Text: utama #E5E7EB, sekunder #9CA3AF
- Tambahkan radial background halus di body (dua radial gradient seperti aura biru/hijau).
- Pastikan kontras teks minimal AA.

3) Tailwind config (tambahkan warna brand + preserve defaults)
- Extend colors.brand 50–900 berbasis primary #179EFF (boleh diset manual).
- Buat util .glass via globals.css (backdrop-blur bg-white/5 border border-white/10).
- Scrollbar tipis.

4) Layout & navigasi
- Layout grid 260px sidebar kiri + main content.
- Sidebar berisi menu: Dashboard (/), Job Board (/jobs), AI Chat (/chat), Analytics (/analytics), Profile (/profile).
- Active state: bg-white/10 text-white; hover: bg-white/5.
- Header kecil di main: “You’re viewing: {PAGE}” + badge “MVP • ICP + Fetch.ai (frontend only)”.
- Animasi transisi konten ringan (opacity + translate-y kecil).

5) Halaman & komponen
- Dashboard:
  - Hero teks singkat (greeting).
  - Grid 4 StatCard—ikon dari lucide-react (Users, Briefcase, DollarSign, Star / TrendingUp, Clock).
  - Panel RecentActivity & ReputationScore; MatchingPreview di bawahnya.
  - Section “Quick Recommendations” dengan tombol “Rekomendasi Cepat” → ambil dari api_client mock dan render 3 kartu JobCard (compact).
- Job Board:
  - Input filter (search by title/skills) + grid JobCard dari array mock.
- Chat:
  - Chat bubble UI sederhana (user: align-right, brand; assistant: glass).
  - Input + Enter-to-send. Balas dari api_client mock (reply: "AI: {prompt}").
- Analytics:
  - 3 kartu metrik random + 1 placeholder chart area (glass).
- Profile:
  - Form ringan (Display Name, Skills) dengan tombol Save (UI only).
- Komponen:
  - StatCard(props: title, value, change, Icon, accent).
  - JobCard(props: job{title,budget,skills,rating,source}, variant).
  - RecentActivity, MatchingPreview, ReputationScore isi copy yang konsisten.
- Semua halaman di “app” folder, client components hanya bila perlu (use client).

6) Aksesibilitas & responsif
- Heading hierarki rapi (h1, h2).
- Focus state tombol/input jelas (ring/outline).
- Mobile: sidebar tetap usable (boleh sticky), grid kolom jadi 1–2.

7) Kode mock API (src/lib/api_client.ts)
- getQuickRecommend(): array 3 item (Golang Backend, AI Agent Integrator, React + ICP Auth).
- sendChat(prompt): kembalikan { reply: "AI: " + prompt } dengan delay kecil (simulasi).

8) Gaya & detail
- Hindari ketergantungan font eksternal (pakai system-ui/Inter optional).
- Tidak pakai komponen berat; cukup Tailwind utility.
- Tidak ada error import/ekspor; pastikan semua file build & run.

Output:
- Hasilkan SELURUH file final: package.json, next.config, tsconfig, postcss, tailwind config, globals.css, Providers.tsx, semua components & pages, dan api_client.ts.
- Pastikan bisa `npm i && npm run dev` langsung jalan (port bebas).
- Desain akhir harus terasa “90° berbeda” dari template lama: sidebar kaca, warna Neo Aura, kartu-kartu rapi, dan interaksi halus.
